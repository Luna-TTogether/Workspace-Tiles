## 目标

降低新建工作区的录入成本，让已有 Chrome 书签的用户可以快速创建工作区。

## 功能范围

- 仅在“新建工作区”中支持从 Chrome 书签添加网站。
- 暂不支持向已有工作区导入书签。
- 暂不支持将工作区导出到 Chrome 书签。
- 用户仍需手动填写工作区名称。
- 未选择书签时，仍可创建空工作区。

## 新建流程

```text
新建工作区
├─ 工作区名称
├─ 从书签添加
│  ├─ [选择书签]
│  └─ 所选文件夹中的网站将被平铺添加，不会保留原有文件夹结构。
├─ 已选择 N 个网站 · [重新选择]
└─ 取消 / 创建
```

- 选择完成后只显示网站数量，不展示网站明细。
- 再次选择时恢复上一次已确认的选择。

## 书签选择器

- 按 Chrome 原始顺序显示书签栏、其他书签、移动设备书签等根目录。
- 文件夹默认折叠，不提供搜索。
- 文件夹和网站均可勾选。
- 文件夹支持全选、半选和取消。
- 勾选文件夹时，递归选中其全部后代网站。
- 用户可以展开文件夹并取消个别网站。
- 点击确认后返回新建工作区弹窗。
- 点击取消时放弃本次修改，保留上一次已确认的选择。

## 导入规则

- 所有网站统一平铺，不保留书签文件夹结构。
- 按 Chrome 书签树的原始顺序进行深度优先导入。
- 两个不同书签即使 URL 相同，也分别保留。
- 同一个书签因父子节点同时被选中，只导入一次。
- 网站名称优先使用书签名称。

## URL 与图标规则

- 书签 URL 按原值导入，不改写格式。
- 手动添加与书签导入统一支持特殊协议。
- 手动输入未带协议时，仍自动补充 `https://`。
- 支持保存和打开 `chrome://`、`file://`、`smb://` 等地址；能否成功打开由 Chrome 和操作系统决定。
- 特殊协议打开失败时保留原数据，不自动修改或删除。
- `javascript:` 书签允许保存和展示，但点击时不执行，并提示“此书签工具需要在目标网页中使用”。
- HTTP/HTTPS 网站使用 Google favicon。
- 非 HTTP/HTTPS 地址不请求 Google favicon，显示网站名称首字母。

## 权限与异常

- 将 `bookmarks` 声明为可选权限。
- 用户首次点击“选择书签”时申请权限。
- 用户拒绝权限时不关闭新建弹窗，并显示：

  > 未获得书签访问权限，你仍可手动添加网站。

- 用户拒绝权限后，手动创建工作区和添加网站功能不受影响。

## 后续范围

- 从书签导入到已有工作区。
- 将工作区导出为 Chrome 书签文件夹。
