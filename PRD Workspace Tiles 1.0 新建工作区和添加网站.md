**产品说明：Chrome 插件，新标签页工作区设置
- **需求存在**：很多人有固定工作流，比如“运营工作区”“开发工作区”“学习工作区”“采购工作区”，他们每天反复打开同一组网站。
- **现有产品过重**：Toby、Workona、Raindrop 都能解决一部分问题，但有账号、同步、复杂管理、权限多、界面重。我不想用，说明这个痛点真实存在。
- **Chrome 原生方案不够顺手**：书签文件夹可以“打开全部”，但视觉弱、入口藏在书签栏，不像新标签页那样自然。

**页面结构**

- 新标签页只显示工作区，不做搜索框
- 背景做接近 Chrome 默认新标签页的浅色/深色自适应
- 没有工作区时，只显示一个“添加工作区”磁贴
- 有工作区时：
    - 工作区按网格展示
    - 最后永远常驻“添加工作区”磁贴

**工作区磁贴**

每个工作区显示：
- 文件夹图标
- 完整名称
- 网站数量
- 最多 `4 x 4 = 16` 个网站 favicon 预览
- 空工作区显示：
    - 文件夹图标
    - 名称
    - `0 个网站`
    - 小的 `+ 添加网站`

交互：
- 点击工作区主体：打开工作区弹窗
- 点击预览 favicon：直接在当前页打开对应网站
- 点击“打开全部”按钮：弹出小菜单
    - 当前窗口打开
    - 新窗口打开

**工作区弹窗**
- 不跳转页面
- 居中大弹窗
- 展示该工作区网站，最多按 `8 x 8 = 64` 一屏布局
- 超过后弹窗内部滚动
- 顶部显示：
    - 工作区名称
    - 网站数量
    - 添加网站
    - 打开全部
    - 关闭
- 网站 icon 可点击打开

**添加**

添加工作区：
- 简单弹窗
- 只填工作区名称

添加网站：
- 简单弹窗
- 填：
    - 名称
    - URL
- URL 自动补 `https://`
- favicon 用 Google favicon 服务
- favicon 失败显示名称首字母

**保存与权限**
- 数据存在 `chrome.storage.local`
- MVP 只本机保存
- 不做同步
- 不做导入导出
- 不做拖拽
- 不做自定义背景
- 不做自定义搜索

**编辑与删除**
- 工作区支持重命名、删除
- 网站支持编辑名称/URL、删除
- 删除工作区或网站前需要二次确认

**打开规则**
- 首页 favicon：直接在当前标签页打开对应网站
- 工作区弹窗内 favicon：直接在当前标签页打开对应网站
- 打开全部：弹出小菜单
    - 当前窗口打开
    - 新窗口打开

**空状态规则**
- 空首页：只显示一个“添加工作区”磁贴
- 空工作区磁贴：显示文件夹图标、完整名称、`0 个网站`、小的 `+ 添加网站`
- 空工作区弹窗：显示 `+ 添加网站`

**URL 与名称规则**
- URL 自动补 `https://`
- URL 为空不能保存
- 网站名称为空时，默认使用域名
- favicon 使用 Google favicon 服务
- favicon 加载失败时显示网站名称首字母

**权限边界**
- MVP 尽量只申请 `storage` 权限
- “新窗口打开全部”使用 `chrome.windows.create`
- 不读取当前标签页，不申请 `tabs` 权限
- 不读取用户浏览历史，不读取网页内容

**数据结构**

```json
{
  "workspaces": [
    {
      "id": "workspace-id",
      "name": "工作区",
      "sites": [
        {
          "id": "site-id",
          "name": "Gmail",
          "url": "https://mail.google.com"
        }
      ]
    }
  ]
}
```

